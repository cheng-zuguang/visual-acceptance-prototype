# DiffLab 原型验证记录

[简体中文](./PROTOTYPE_NOTES.md) | [English](./PROTOTYPE_NOTES.en.md)

> PROTOTYPE — 这份代码用于回答产品与算法问题，不应直接作为生产实现发布。

## 要回答的问题

给定一个 Figma Frame（或基准图片）、一个公开 H5 URL 和一组 YAML 验收规则，能否稳定生成：

1. 可复现的通过/不通过结论；
2. 能定位到页面区域和 DOM 元素的差异；
3. 设计、产品和开发都能理解的改进建议与证据报告。

## 已确定的原型边界

- 一次比对一个 Frame、一个视口和默认静态页面状态。
- Figma Frame 尺寸自动成为 H5 截图视口，DPR 固定为 1。
- 支持 Figma Frame 链接或 PNG/JPG/WebP；不解析 `.fig`。
- H5 必须无需登录即可访问。
- 规则为唯一门禁来源；AI 仅生成解释，不决定通过与否。
- 精确 `data-figma-id`、文本/几何自动匹配和置信度降级并存。
- 无数据库、用户系统或历史任务；凭据以浏览器 `localStorage` 明文缓存，只随当前请求发送，并可在页面中一键清除。

## 当前验证结果

- 内置样本可以端到端完成基准截图、H5 截图、节点匹配、规则执行和报告生成。
- 内置样本中故意加入的布局、排版、颜色、圆角和缺失元素差异会导致不通过。
- 可生成左右对照、透明叠加、差异热图、HTML、Markdown、JSON 和 PNG。
- 支持自定义 AI API 基础地址或完整 `/responses` 地址，并允许无 Key 的本地兼容服务。
- 支持从兼容的 `/models` 端点获取模型列表、选择或手动输入模型，并在报告中记录实际使用的模型。
- 模型列表使用应用内可搜索面板展示；凭据、已选模型与最近一次模型列表在刷新后可恢复。
- 生产构建和 TypeScript 类型检查通过。

## 尚未回答

- 真实业务页面上的自动匹配误报率与漏报率。
- 字体、图片资源和复杂 Figma Auto Layout 在不同环境中的噪声范围。
- 第一批真实样本适合采用的最终阈值。

真实样本验证后，应在这里记录结论，并决定删除原型或将有效设计重写进正式项目。
